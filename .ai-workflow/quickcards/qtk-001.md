# 轻量卡: QTK-001 线框可视化增强为平行光照加深度测试

## QuickCardId
`QTK-001`

## Type
`QuickTask`

## Goal
在不改公共契约和不引入完整材质/灯光系统的前提下，让当前线框 shader 结果更直观，优先尝试“最小平行光照效果 + 深度测试”。

## Reporter
`Human`

## Owner
`pending`

## Priority
`P2`

## PrimaryModule
`Engine.Render`

## RelatedPlan
`PLAN-M10-2026-04-25`

## RelatedTaskId
`TASK-APP-009`

## Summary
- 当前线框 shader 可视化不直观，用户希望尝试更容易读形体的最小方案。
- 先限定为 Render 单模块增强：不引入场景灯对象，不扩展材质系统，不新增公开契约。

## Expected
- 线框或当前几何显示结果比现状更容易辨识体块前后关系。
- 至少尝试启用深度测试，并评估一个最小固定方向平行光照/明暗变化方案。
- 保持现有主路径可运行，不引入新的跨模块输入要求。

## Actual
- 当前线框 shader 视觉上不够直观，用户难以判断体块关系与朝向。

## Repro
- 运行当前默认场景。
- 观察现有线框或几何显示结果。
- 对比修改前后的可辨识度。

## ScopeLimit
- 仅允许改动 `Engine.Render` 内的 shader / render state / 最小可视化逻辑。
- 明确不做：
  - 不引入正式灯光系统
  - 不新增 `Engine.Contracts` 光照契约
  - 不修改 `Scene/App` 传参与配置结构
  - 不做大范围渲染管线重构

## Acceptance
- Build: 至少完成一次最小编译/构建验证
- Test: 至少一条 Render 相关验证证据，或明确“无自动化测试，仅人工可视对比”
- Smoke: 至少完成一次修改前后画面对比并记录结论

## EscalationRule
- 若触发以下任一条件，必须升级为正式任务卡：
  - 需要 `Scene` 提供灯光数据或新增 `Engine.Contracts` 字段
  - 需要边界文档更新
  - 预计超过半天
  - 风险达到 `medium/high`
  - 需要从“可视化增强”扩展成正式 lighting/shading 功能

## Evidence
- 用户反馈：当前线框 shader 不直观
- 候选方案：固定平行光照 + 深度测试

## Status
`Todo`

## Completion
`0`

## Escalation
- EscalatedToTaskId:
- EscalationReason:
